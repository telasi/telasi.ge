<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
    <%= t('models.general.more') %>
    <b class="caret"></b>
  </a>
  <ul class="dropdown-menu">
    <!-- public applications -->
    <li><%= link_to t('menu.new_customer'), new_customers_url %></li>
    <li><%= link_to t('menu.payments'), pay_services_url %></li>

    <!-- network application -->
    <%if current_user and Sys::Permission.has_permission_for_path?(current_user,network_home_path)%>
      <li class="divider"></li>
      <li><%= link_to t('applications.admin.network'), network_home_url %></li>
    <%end%>

    <!-- news administration -->
    <%if current_user and Sys::Permission.has_permission_for_path?(current_user,admin_subscriptions_path)%>
      <li class="divider"></li>
      <li><%= link_to t('applications.admin.subscriptions'), admin_subscriptions_url %></li>
    <%end%>

    <!--customer confirmations-->
    <%if current_user and Sys::Permission.has_permission_for_path?(current_user,admin_customers_url)%>
      <li class="divider"></li>
      <li><%= link_to t('applications.admin.customers'), admin_customers_url %></li>
    <%end%>

    <!-- admin applications -->
    <%if current_user and current_user.admin?%>
      <li class="divider"></li>
      <li><%= link_to t('applications.admin.users'), admin_users_url %></li>
    <%end%>
  </ul>
</li>