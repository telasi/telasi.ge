<%= render partial: 'new_customers/top' %>

<% if @application.editable_online? %>
  <div class="btn-toolbar" style="margin-bottom: 16px;"> 
    <a href="<%= new_customer_upload_file_url(id: @application.id) %>" class="btn btn-success">
      <i class="fa fa-plus-circle"></i>
      <%= t 'models.network_new_customer_application.actions.upload' %>
    </a>
  </div>
<% end %>

<% if @application.files.any? %>
<table class="table table-hover table-bordered">
  <thead>
    <tr>
      <th>#</th>
      <th>თარიღი, დრო</th>
      <th>ფაილის დასახელება</th>
      <th>ფაილის ზომა</th>
      <th class="text-center"><i class="fa fa-cog"></i></th>
    </tr>
  </thead>
  <tbody>
    <% @application.files.each_with_index do |file, index| %>
      <tr>
        <td class="text-right">
          <%= index + 1 %>
        </td>
        <td><%= file.created_at.strftime('%d/%m/%Y %H:%M:%S') %></td>
        <td>
          <a href="<%= file.file.to_s  %>">
            <%= file.file.filename %>
          </a>
        </td>
        <td class="text-right">
          <code><%= (file.file.size / 1000.0).round %></code> kB
        </td>
        <td class="text-center">
          <% if @application.editable_online? %>
            <a href="<%= new_customer_delete_file_url(id: file.id) %>" data-method="delete" class="btn btn-sm btn-default" data-confirm="<%= t 'models.network_new_customer_application.actions.delete_confirm' %>">
              <i class="fa fa-trash-o"></i>
            </a>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
<!--div class="well">
  ჯერ არც ერთი დოკუმენტი არაა წარმოდგენილი.
  დოკუმენტის დასამატებლად მიაჭირეთ ღილაკს "დოკუმენტის დამატება".
</div-->
<% end %>

<% if @application.not_sent? %>
  <% if not @application.can_send? %>
    <h3 class="page-header">აუცილებელი დოკუმენტების სია</h3>
    <div class="alert alert-info">
      ქვემოთ მოცემულია თუ რა დოკუმენტების დამატებაა აუცილებელი განცხადების გასაგზავნად.
    </div>
    <!-- project documentation -->
    <% if @application.doc_project %>
      <h3 class="text-success page-header">
        ნაბიჯი #1: ატვირთეთ პროექტი <%= yes_no true %>
      </h3>
    <% else %>
      <h3 class="text-danger page-header">
        ნაბიჯი #1: ატვირთეთ პროექტი <%= yes_no false %>
      </h3>
      <ol>
        <li>თქვენ უნდა ფლობდეთ ქალაქის არქიტექტურული სამსახურის მიერ დამტკიცებულ პროექტს;</li>
        <li>დაასკანრეთ თქვენი პროექტი და მასთან დაკავშირებული ყველა დოკუმენტაცია და მოათავსეთ ერთ ფაილში თქვენს კომპიუტერზე;</li>
        <li>დააჭირეთ მწვანე ღილაკს წარწერით "დოკუმენტის დამატება" და ატვირთეთ პროექტის ფაილი თქვენი კომპიუტერიდან;</li>
        <li>
          როდესაც დაასრულებთ პროექტის ატვირთვას, დააჭირეთ შესრულების ღილაკს:
        </li>
      </ol>
      <div class="text-center">
        <a href="<%= new_customer_confirm_step_url(id: @application.id, step: 'doc_project') %>" data-method="post" data-confirm="დაადასტურეთ, რომ ნამდვილად ატვირთეთ პროექტის დოკუმენტი!" class="btn btn-success">
          <i class="fa fa-check"></i>
          ნაბიჯი #1 შესრულდა
        </a>
      </div>
    <% end %>
    <!-- payment documentation -->
    <% if @application.doc_payment %>
      <h3 class="text-success page-header">
        ნაბიჯი #2: გადაიხადეთ ღირებულების 50% <%= yes_no true %>
      </h3>
    <% else %>
      <h3 class="text-danger page-header">
        ნაბიჯი #2: გადაიხადეთ ღირებულების 50% <%= yes_no false %>
      </h3>
      <ol>
        <li>
          ქსელზე მიერთების ღირებულება თქვენს შემთხვევაში შეადგენს <code><%= @application.amount %></code> GEL.
          თქვენ უნდა გადაიხადოთ მინიმუმ ამ თანხის <code>50</code>%;
        </li>
        <li>
          <%= link_to 'ამობეჭდეთ საგადახდო დავალება', new_customer_paybill_url(id: @application.id, format: 'pdf') %>
          და გადაიხადეთ მითითებული თანხა ბანკში;
        </li>
        <li>
          გადაიღეთ (დაასკანერეთ) ბანკის მიერ გაცემული გადახდის დამადასტურებელი საბუთი და
          შეინახეთ თქვენს კომპიუტერზე;
        </li>
        <li>
          დაამატეთ გადახდის დამადასტურებელი დოკუმენტი თქვენს განცხადებაში. ამისთვის გამოიყენეთ დიდი მწვანე ღილაკი წარწერით "დოკუმნეტის დამატება";
        </li>
        <li>
          როდესაც დაასრულებთ ყველა ზემოთ აღწერილ ნაბიჯს, დააჭირეთ შესრულების ღილაკს:
        </li>
      </ol>
      <div class="text-center">
        <a href="<%= new_customer_confirm_step_url(id: @application.id, step: 'doc_payment') %>" data-method="post" data-confirm="დაადასტურეთ, რომ ნამდვილად ატვირთეთ გადახდის დოკუმენტი!" class="btn btn-success">
          <i class="fa fa-check"></i>
          ნაბიჯი #2 შესრულდა
        </a>
      </div>
    <% end %>
  <% else %>
    ???
  <% end %>
<% end %>