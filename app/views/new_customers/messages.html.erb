<%= render partial: 'new_customers/top' %>
<div class="text-muted" style="margin-bottom: 16px;">
  შეტყობინებები მოგივათ SMS საშუალებით თქვენს მობილურ ტელეფონზე.
</div>
<% if @application.messages.any? %>
<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>თარიღი, დრო</th>
      <th>შეტყობინების ტექსტი</th>
      <th>მობილურის #</th>
    </tr>
  </thead>
  <tbody>
    <% @application.messages.each do |msg| %>
      <tr>
        <td><%= msg.created_at.strftime('%d/%m/%Y %H:%M:%S') %></td>
        <td><%= msg.message %></td>
        <td><code><%= KA.format_mobile msg.mobile %></code></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
<div class="well">
  ჯერ არც ერთი შეტყობინება არ არის!
</div>
<% end %>